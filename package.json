{
  "name": "web2slides",
  "version": "1.0.0",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/popup.js src/background.js src/content.js --bundle --outdir=dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "turndown": "^7.2.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
